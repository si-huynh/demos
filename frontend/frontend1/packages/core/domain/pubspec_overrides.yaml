# melos_managed_dependency_overrides: common,data,models,network
dependency_overrides:
  common:
    path: ../common
  data:
    path: ../data
  models:
    path: ../models
  network:
    path: ../network
